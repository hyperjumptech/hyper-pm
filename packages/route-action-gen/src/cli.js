#!/usr/bin/env node
/**
 * Minimal CLI placeholder. Full generation lives in the published Hyperjump toolchain.
 */
console.log(
  "route-action-gen: workspace stub — run generators from the Hyperjump docs when available.",
);
process.exit(0);
