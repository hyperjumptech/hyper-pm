{
  "name": "hyper-pm-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && esbuild src/main.ts --bundle --platform=node --format=esm --outfile=dist/server.mjs",
    "check-types": "tsc --noEmit -p tsconfig.json",
    "dev": "tsx watch src/main.ts",
    "lint": "eslint \"src/**/*.ts\"",
    "start": "node dist/server.mjs",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@workspace/env": "workspace:*",
    "@workspace/hyper-pm-cli-runner": "workspace:*",
    "hyper-pm": "workspace:^"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "@workspace/eslint-config": "workspace:*",
    "@workspace/typescript-config": "workspace:*",
    "esbuild": "^0.27.0",
    "eslint": "catalog:",
    "tsx": "^4.19.2",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
