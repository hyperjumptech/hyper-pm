{
  "extends": ["//"],
  "tasks": {
    "test": {
      "dependsOn": ["^db:generate", "^build", "generate", "build"]
    },
    "test:coverage": {
      "dependsOn": [
        "^db:generate",
        "^build",
        "^test:coverage",
        "generate",
        "build"
      ]
    }
  }
}
